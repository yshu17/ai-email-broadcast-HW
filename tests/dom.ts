import "@testing-library/jest-dom/vitest";

/**
 * Loaded by the tests that run in a DOM (`// @vitest-environment jsdom`): the matchers that read
 * like what a person sees (`toBeInTheDocument`, `toHaveFocus`, `toHaveTextContent`, ...).
 */
